{
  "name": "cloudflare-saas-stack",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "pages:build": "npx @cloudflare/next-on-pages",
    "preview": "npm run pages:build && wrangler pages dev",
    "deploy": "npm run pages:build && wrangler pages deploy",
    "cf-typegen": "wrangler types --env-interface CloudflareEnv env.d.ts",
    "deploy:full": "npm run lint && npm run db:migrate:prod && npm run pages:build && wrangler pages deploy",
    "deploy:staging": "npm run pages:build && wrangler pages deploy --env staging",
    "ci:build": "npm run lint && npm run pages:build",
    "ci:deploy": "npm run db:migrate:prod && wrangler pages deploy",
    "setup": "npx tsx scripts/setup.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate:dev": "drizzle-kit migrate",
    "db:migrate:prod": "NODE_ENV=production drizzle-kit migrate",
    "db:studio:dev": "drizzle-kit studio",
    "db:studio:prod": "NODE_ENV=production drizzle-kit studio",
    "studio:start": "drizzle-kit studio > /dev/null 2>&1 & echo $! > .studio.pid && echo 'Drizzle Studio started at https://local.drizzle.studio'",
    "studio:stop": "if [ -f .studio.pid ]; then kill $(cat .studio.pid) && rm .studio.pid && echo 'Drizzle Studio stopped'; else echo 'Drizzle Studio is not running'; fi",
    "studio:restart": "npm run studio:stop && npm run studio:start",
    "db:query": "npx wrangler d1 execute atenra-dev-db --local --command",
    "db:query:prod": "npx wrangler d1 execute atenra-dev-db --command"
  },
  "dependencies": {
    "@auth/drizzle-adapter": "^1.10.0",
    "@clack/prompts": "^0.11.0",
    "@iarna/toml": "^2.2.5",
    "@libsql/client": "^0.10.0",
    "@radix-ui/react-icons": "^1.3.0",
    "@radix-ui/react-label": "^2.1.7",
    "@radix-ui/react-slot": "^1.2.3",
    "@t3-oss/env-nextjs": "^0.11.1",
    "@types/bcryptjs": "^3.0.0",
    "bcryptjs": "^3.0.2",
    "class-variance-authority": "^0.7.0",
    "clsx": "^2.1.1",
    "drizzle-kit": "^0.24.2",
    "drizzle-orm": "^0.33.0",
    "lucide-react": "^0.438.0",
    "next": "14.2.5",
    "next-auth": "^5.0.0-beta.29",
    "react": "^18",
    "react-dom": "^18",
    "tailwind-merge": "^3.3.1",
    "tailwindcss-animate": "^1.0.7",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@cloudflare/next-on-pages": "^1.13.15",
    "@cloudflare/workers-types": "^4.20240903.0",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "eslint": "^8",
    "eslint-config-next": "14.2.5",
    "eslint-plugin-next-on-pages": "^1.13.2",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "typescript": "^5",
    "vercel": "^37.3.0",
    "wrangler": "^4.33.1"
  }
}
