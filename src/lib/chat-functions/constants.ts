/**
 * Shared constants for chat functions
 */

/**
 * Time threshold in seconds for determining if a user is "online"
 * If lastActiveAt is within this many seconds of now, the user is considered online
 */
export const ONLINE_THRESHOLD_SECONDS = 60;
